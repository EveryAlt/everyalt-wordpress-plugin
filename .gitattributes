# Files that live in the repo but must NOT ship inside the plugin ZIP.
# `git archive` (used by build.sh) honors these export-ignore rules.
/.gitattributes export-ignore
/.gitignore     export-ignore
/build.sh       export-ignore
/builds/        export-ignore
